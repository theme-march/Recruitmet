import {prisma} from "@/lib/prisma"; import {getSession} from "@/lib/session"; import {can} from "@/lib/authorization"; import {AppError,errorResponse} from "@/lib/errors"; import {parsePagination,pageResult} from "@/lib/pagination"; import {leadCreateSchema} from "@/features/leads/schemas"; import {createLead} from "@/features/leads/service";
export async function GET(request:Request){try{const session=await getSession();if(!session)throw new AppError("UNAUTHORIZED","Sign in is required.",401);if(!(await can(session,"call-center","View")))throw new AppError("FORBIDDEN","View permission is required.",403);const p=parsePagination(request.url),where=p.q?{OR:[{leadNo:{contains:p.q}},{fullName:{contains:p.q}},{phone:{contains:p.q}}]}:{};const[data,total]=await Promise.all([prisma.workCall.findMany({where,skip:p.skip,take:p.take,orderBy:{createdAt:"desc"},select:{id:true,leadNo:true,fullName:true,phone:true,country:true,workCategory:true,priority:true,status:true,followUpAt:true,assignedTo:{select:{name:true}},createdAt:true}}),prisma.workCall.count({where})]);return Response.json(pageResult(data,total,p.page,p.pageSize))}catch(error){return errorResponse(error)}}
export async function POST(request:Request){try{const session=await getSession();if(!session)throw new AppError("UNAUTHORIZED","Sign in is required.",401);const lead=await createLead(leadCreateSchema.parse(await request.json()),session);return Response.json({data:{id:lead.id,leadNo:lead.leadNo}},{status:201})}catch(error){return errorResponse(error)}}
