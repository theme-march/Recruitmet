import {z} from "zod"; import {getSession} from "@/lib/session"; import {AppError,errorResponse} from "@/lib/errors"; import {verifyDocument} from "@/features/documents/verification-service";
const schema=z.object({status:z.enum(["VERIFIED","REJECTED"]),reason:z.string().min(3).max(500).optional()}).refine(x=>x.status!=="REJECTED"||x.reason,{message:"A rejection reason is required",path:["reason"]});
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const session=await getSession();if(!session)throw new AppError("UNAUTHORIZED","Sign in is required.",401);const{id}=await params,input=schema.parse(await request.json());const row=await verifyDocument(id,input.status,input.reason,session);return Response.json({data:{id:row.id,status:row.status}})}catch(error){return errorResponse(error)}}
