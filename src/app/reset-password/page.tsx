"use client";
import {useState,Suspense} from "react"; import {useSearchParams,useRouter} from "next/navigation"; import Link from "next/link";
function ResetForm(){const params=useSearchParams(),router=useRouter(),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:params.get("token"),password:data.password,confirmPassword:data.confirmPassword})});const body=await response.json();if(!response.ok){setError(body.error?.message||"Reset failed");return}router.replace("/login")}return <form className="auth-panel" onSubmit={submit}><span>SECURE RESET</span><h1>Choose a new password</h1><p>Use at least 10 characters with upper/lowercase, a number and a symbol.</p>{error&&<div className="form-error">{error}</div>}<label>New password<input name="password" type="password" required/></label><label>Confirm password<input name="confirmPassword" type="password" required/></label><button className="login-button">Reset password</button><Link href="/login">Back to sign in</Link></form>}
export default function Page(){return <div className="auth-simple"><Suspense fallback={<div>Loading…</div>}><ResetForm/></Suspense></div>}
