"use client";
import {useState} from "react"; import Link from "next/link";
export default function Page(){const [message,setMessage]=useState(""),[token,setToken]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const identity=String(new FormData(e.currentTarget).get("identity"));const response=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({identity})});const body=await response.json();setMessage(body.message||body.error?.message);setToken(body.developmentToken||"")}return <div className="auth-simple"><form className="auth-panel" onSubmit={submit}><span>ACCOUNT RECOVERY</span><h1>Forgot password</h1><p>Enter your email or username. Recovery instructions expire after 30 minutes.</p><label>Email or username<input name="identity" required/></label><button className="login-button">Send reset instructions</button>{message&&<div className="auth-message">{message}</div>}{token&&<Link className="dev-reset" href={`/reset-password?token=${token}`}>Development only: continue to reset</Link>}<Link href="/login">Back to sign in</Link></form></div>}
